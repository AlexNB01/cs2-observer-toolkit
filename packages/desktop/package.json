{
  "name": "@cs2hud/desktop",
  "private": true,
  "version": "0.2.6",
  "description": "CS2 broadcast companion — GSI setup, Smart Auto Observer, and HLAE integration.",
  "author": "CS2 Observer Toolkit",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && electron .",
    "typecheck": "tsc --noEmit",
    "dist": "npm run build -w @cs2hud/web && npm run build && electron-builder"
  },
  "dependencies": {
    "@cs2hud/server": "*",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "43.2.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0"
  }
}
